interface:
  display_name: "WhatToEat"
  short_description: "Repo-specific patterns and workflows for whatToEat"
  default_prompt: "Use the whatToEat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true